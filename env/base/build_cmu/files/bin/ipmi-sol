#!/bin/sh
if [ "x$1" != "x" ] ; then
	echo ipmitool -H $1 -U admin -P admin -I lanplus sol activate
	ipmitool -H $1 -U admin -P admin -I lanplus sol activate || \
	ipmitool -H $1 -U admin -P admin -I lanplus sol deactivate
else
	echo Please supply the host name as the one and only parameter.
fi
